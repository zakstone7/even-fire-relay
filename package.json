{
  "name": "fire-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Self-hostable HTTP relay for the Fire Even G2 plugin (bypasses WebView CORS).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
